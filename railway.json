{
  "build": {
    "builder": "nixpacks"
  },
  "deploy": {
    "startCommand": "npm start",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  },
  "nixpacks": {
    "startCmd": "npm start",
    "buildCmd": "echo 'No build needed - server only' && exit 0",
    "phases": {
      "build": {
        "cmds": ["echo 'Skipping build phase'"]
      },
      "start": {
        "cmds": ["npm start"]
      }
    }
  }
}
