{
  "build": {
    "builder": "nixpacks"
  },
  "deploy": {
    "startCommand": "npm start",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10,
    "healthcheckPath": "/health",
    "healthcheckTimeout": 300
  },
  "nixpacks": {
    "startCmd": "npm start",
    "buildCmd": "echo 'Build phase disabled' && exit 0"
  }
}
